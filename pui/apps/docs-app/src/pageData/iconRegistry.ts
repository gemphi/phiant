import { BookOpen, Play, Layers, Search, Settings, Bell, Home, User, Heart, MessageCircle, Share2, MoreHorizontal, ThumbsUp, ThumbsDown, Cast } from 'lucide-react';

export const ICON_REGISTRY: Record<string, React.ComponentType<any>> = {
  BookOpen,
  Play,
  Layers,
  Search,
  Settings,
  Bell,
  Home,
  User,
  Heart,
  MessageCircle,
  Share2,
  MoreHorizontal,
  ThumbsUp,
  ThumbsDown,
  Cast,
};
