# Core

Phient platform `Core` documentation and schemas.

## Models
All schema definitions and data models for `Core` are available in [`./models/`](./models/README.md).

### Example
```python
from phiegg import PhiEggClient

client = PhiEggClient()
```
